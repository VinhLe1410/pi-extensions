{
  "name": "pi-extensions",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests --exclude \"pi-subagents/test/**\""
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "^0.80.2",
    "@earendil-works/pi-coding-agent": "^0.80.2",
    "@earendil-works/pi-tui": "^0.80.2",
    "@types/node": "25.7.0",
    "typebox": "^1.1.33",
    "typescript": "^5.0.0",
    "vitest": "^4.1.5"
  }
}
